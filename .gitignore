node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
service-account.json
data/*.xlsx

# Cached gear images are committed so they ship with the deployment —
# Amazon blocks Cloud Run IPs with captcha, so runtime scraping fails in
# prod and the fallback icon shows instead of the real product image.

# Claude Code per-project settings
.claude/
