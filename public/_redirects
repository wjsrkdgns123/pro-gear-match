# Pre-rendered per-route HTML (scripts/generate_route_html.mjs writes
# dist/<route>/index.html for every SPA path) handles all current routes
# automatically — Cloudflare Pages serves the directory index. Keeping
# _redirects empty avoids conflicts that previously caused 307 redirects to /.
